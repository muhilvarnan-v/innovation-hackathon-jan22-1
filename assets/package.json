{
  "scripts": {
    "deploy_tailwind": "NODE_ENV=production tailwindcss --minify -i css/app.css -o ../priv/static/assets/app.css",
    "deploy": "cd .. && mix assets.deploy"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.7"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.4.0"
  }
}
